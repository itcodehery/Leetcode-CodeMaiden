impl Solution {
    pub fn num_subarray_product_less_than_k(nums: Vec<i32>, k: i32) -> i32 {
        let super_array: Vec<Vec<i32>>;
        let element_counter: i32 = 0;
        for i in 0..2 ^ nums.len() {
            super_array.push();
        }

        0
    }
}

fn main() {
    println!("Hello, world!");
}
